*** a/components/JobCard.tsx
--- b/components/JobCard.tsx
@@
-import { clsx } from "clsx";
+// (removed unused import)

 export type Job = {
   id: string | number;
   title: string;
   company: string;
   location?: string;
   postedAt?: string; // ISO date string
   tags?: string[];
   url?: string;
   salary?: string;
   type?: string; // e.g., Full-time
 };

 export default function JobCard({ job }: { job: Job }) {
   return (
     <article className="card p-5 md:p-6">
@@
       <div className="mt-5 flex items-center gap-3">
-        {job.salary && <span className="text-sm font-medium text-ink">{job.salary}</span>}
+        {job.salary && <span className="text-sm font-medium text-ink">{job.salary}</span>}
         {job.url && (
           <a className="btn" href={job.url} target="_blank" rel="noreferrer">
             View Job
           </a>
         )}
       </div>
*** a/app/globals.css
--- b/app/globals.css
@@
-:root {
-  --radius: 1rem;
-}
-
-html, body {
-  height: 100%;
-  background: theme(colors.surface.DEFAULT);
-  color: theme(colors.ink.soft);
-}
+:root { --radius: 1rem; }
+html, body { @apply h-full bg-surface text-ink-soft; }
 
 @layer base {
   h1 { @apply text-4xl md:text-5xl font-semibold tracking-tight text-ink; }
   h2 { @apply text-2xl md:text-3xl font-semibold text-ink; }
   h3 { @apply text-xl font-semibold text-ink; }
*** a/package.json
--- b/package.json
@@
   "devDependencies": {
-    "autoprefixer": "10.4.19",
-    "postcss": "8.4.38",
-    "tailwindcss": "3.4.10",
-    "typescript": "5.5.4",
-    "eslint": "9.9.0",
-    "eslint-config-next": "14.2.5"
+    "autoprefixer": "10.4.19",
+    "postcss": "8.4.38",
+    "tailwindcss": "3.4.10",
+    "typescript": "5.5.4",
+    "eslint": "8.57.0",
+    "eslint-config-next": "14.2.5"
   }
